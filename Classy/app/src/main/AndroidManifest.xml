<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lucis.classy">

    <application
        android:name=".Noctis"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".StudentActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".TeacherActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".FeedbackActivity" />
        <activity android:name=".LearnPaceActivity" />
        <activity android:name=".ModuleActivity" />
        <activity android:name=".CreateActivity" />
        <activity android:name=".SelectModuleActivity" />
        <activity android:name=".ViewActivity" />
        <activity android:name=".viewLessonActivity" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".SecretView"
            android:label="EASTER EGG BAYBEEEE"></activity>
    </application>

</manifest>